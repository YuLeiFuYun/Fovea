# 安全政策

## 当前支持范围

Fovea 仍处于 `0b-in-progress`，尚无稳定发布分支。安全修复以当前 `main` 为基线；历史提交与未发布实验能力不承诺长期补丁支持。

## 报告漏洞

请不要在公开 issue、讨论区、日志或样例中提交可利用细节、凭证、私有 URL、账户标识或缓存内容。优先使用仓库托管平台提供的私有漏洞报告通道；若该通道未启用，请通过项目所有者已经公布的私有联系方式报告。仓库没有公布的联系方式时，不应猜测地址或把漏洞公开化。

报告应尽量包含：

- 受影响提交、平台、系统和 Swift/Xcode 版本；
- 最小复现与预期/实际安全边界；
- 是否涉及跨 namespace 复用、凭证泄漏、路径/link 攻击、资源耗尽或持久格式损坏；
- 已做的隔离措施；
- 不含真实秘密的测试数据。

## 处理原则

- 先确认可利用边界，再最小化修复并增加回归测试；
- 默认 fail closed，不通过扩大缓存复用或降低校验来“修复”兼容性；
- 安全相关缓存键、持久 schema 或默认策略变化必须记录迁移影响；
- 发布前不得把本地 smoke、AI 自评或未运行的 CI lane 表述为独立安全审计。
